# Data + cache are reproducible from the pull scripts; don't commit them.
# (The written briefs, the self-contained deck, and the figures in outputs/
# ARE committed — figures must be in-repo so the rebuild-deck workflow can
# embed them when the deck script is edited on github.com.)
data/cache/
data/raw/
data/processed/

# Local tool config
.claude/

# Secrets / local environment
.env
.env.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/
